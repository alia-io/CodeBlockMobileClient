<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.codeblockmobileclient">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--<meta-data
        android:name="com.google.firebase.messaging.default_notification_icon"
        android:resource="@drawable/ic_notification" />-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.CodeBlockMobileClient">
        <service
            android:name=".communication.SocketMessageService"
            android:description="@string/socket_service_description"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".communication.ConnectClientActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".SignupLoginActivity"
            android:exported="true" />
    </application>

</manifest>