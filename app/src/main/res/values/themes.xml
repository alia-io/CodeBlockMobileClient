<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Theme.CodeBlockMobileClient" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:windowBackground">@color/background</item>
        <!--<item name="android:textColor">@color/text</item>-->
        <item name="colorPrimary">@color/keyword</item>
        <item name="colorPrimaryVariant">@color/function</item>
        <item name="colorPrimaryDark">@color/variable</item>
        <item name="colorSecondary">@color/annotation</item>
        <item name="colorSecondaryVariant">@color/error</item>
        <item name="colorAccent">@color/string</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/comment</item>
        <item name="actionBarStyle">@style/Theme.CodeBlockMobileClient.ActionBar</item>
        <item name="android:itemBackground">?attr/colorPrimaryVariant</item>
        <item name="textInputStyle">@style/Theme.CodeBlockMobileClient.TextInputLayout</item>
    </style>

    <!-- Action bar style -->
    <style name="Theme.CodeBlockMobileClient.ActionBar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="background">?attr/colorPrimary</item>
    </style>

    <!-- Text input layout style -->
    <style name="Theme.CodeBlockMobileClient.TextInputLayout" parent="Widget.Design.TextInputLayout">
        <item name="android:background">@drawable/text_input_layout_style</item>
        <item name="android:layout_marginLeft">20dp</item>
        <item name="android:layout_marginRight">20dp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingBottom">5dp</item>
        <item name="android:paddingLeft">20dp</item>
        <item name="android:paddingRight">20dp</item>
    </style>

</resources>